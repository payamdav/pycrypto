# Agents Folder plus Candle Access Instruction
## Agents Folder
> Create a folder named "agents" that will be used to store markdown files for AI Agents that describes projects and its toolings
> create subfolder for that as:
* datasets
* packages
* ideas
* rules

## Huggingface Candles instruction
> inside /agents/datasets/ create a markdown file "huggingface_candles.md" and inside that describe how to access payamdavaee/candles dataset on huggingface. also include the structure of those parquet files. be aware that we have 7 assets folders there that each one has monthly files of minutes candles there. so include the column names there with short or just few words description.
the users of these file are AI programming Agents so write in a way that is best suitable for them. short and meaningful.
also create an assets.md that includes lists of 7 assets that we have and include these file in huggingface_candles.md
the provided instruction must include all required information that may needed including options to reach data (huggingface_hub, duckdb, download, ...) , urls, file naming convention.
so after that I may instruct the Ai agents to get vwap and aggressive buy volume of some asset from that date to other. then the agents must know what to do


